# Weather-app

Plain HTML/CSS/JavaScript weather lookup app that fetches current conditions from OpenWeather.

Run locally: add your OpenWeather API key in `config.js`, then open `index.html` in a browser.